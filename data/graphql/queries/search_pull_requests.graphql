query SearchPullRequests($searchQuery: String!, $limit: Int = 10) {
    search(query: $searchQuery, type: ISSUE, first: $limit) {
        edges {
            node {
                __typename
                ...pullRequestFields
            }
        }
    }
}

fragment pullRequestFields on PullRequest {
    databaseId
    number
    title
    url
    state
    updatedAt
    createdAt
    assignees(first: 10) {
        nodes {
            login
        }
    }
    labels(first: 10) {
        nodes {
            name
        }
    }
    repository {
        nameWithOwner
    }
    comments {
        totalCount
    }
    reactions {
        totalCount
    }
}